import React, { useEffect, useState, useRef } from 'react';

export default function Lista() {
  // Aqui você pode inicializar os estados e usar hooks como useEffect e useRef, se necessário.

  return (
    <div>
      Oi
    </div>
  );
}
